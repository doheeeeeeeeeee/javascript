// 조건 condition
//if ,e


{
    const age =prompt('당신의 나이는?')
    if(조건) {
        //  //실행
        // } else {
        //  // 실행
        // }

        if(age>= 20){
            console.log(`성인입니다.`)
        }  else{
            console.log(`청소년입니다.`)
        }
    }
}

{
    const month = prompt(`지금은 몇월입니까?`)

    if(month >+3 && month <=5) {
        console.log('봄 입니다.');
    }  esle if(month >= 6 && month <=8) {
        console.log('여름 입니다.');
    } esle if(month >= 9 && month <=8) {
        console.log('가을 입니다.');
    } esle {
        console.log('겨울 입니다.');
    }
}

//2.switch 문 
switch (key) {
    case Value: 
        //실행
        break;
    case value2:
        //실행
        break;
    default:
        break;
}

if(url) {
    const message =`당신은 < a `
}
